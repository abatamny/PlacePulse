# Load tests

Load, queue-fairness, WebSocket, and upload stress suites are introduced with
the runtime features they exercise. Milestones 0–4 intentionally contain no
worker, polling loop, or synthetic queue implementation.
